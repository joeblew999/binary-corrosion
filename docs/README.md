# README

We use Takskfile and rust cross to locally build the rust code for Linux, Mac and Windows locally.

We use the same Taskfile in github CI, but because there is no Docker environmnet, we use per OS builds.

